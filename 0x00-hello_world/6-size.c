#include <stdio.h>
/**
 * Main - A program that prints the size of various types on the computer it is compiled and run on
 * Return 0 (Success)
 */
int main(void)
{
	char a;
	int b;
	long int c;
	long long int d;
	float e;

	printf("size of a char %lu", (unsigned long)size of (a));
	printf("size of an int %lu", (unsigned long)size of (b));
	printf("size of a long int %lu", (unsigned long)size of (c));
	printf("size of a long long int %lu", (unsigned long)size of (d));
	printf("size of a float %lu", (unsigned long)size of (e));
	return (0);
}	
