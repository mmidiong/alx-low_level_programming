#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	int m;
	char p;

	m = 0;
	p = 'a';

	while
	(m <= 9)
	{
	putchar(m + '0');
	m++;
	} while

	(p <= 'f')
	{
	putchar(p);
	p++;
	}

	putchar('\n');

	return (0);
}
